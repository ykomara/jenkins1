pipeline {
    agent any  

    tools {
        gradle 'gradle9.4.0'
    }

    stages {
        stage ('Build') {
            steps {
                sh 'gradle -v'
            }

        }

        
    }
}
