pipeline {
    // Use any available agent
    agent any

    environment {
        // Define any custom environment variables here if needed
        MY_VAR = 'some_value'
        MY_NUMBER = '123'
    }

    stages {
        stage('Build') {
            steps {
               
                sh 'npm -v' 
            }

        }
        
    }



}
